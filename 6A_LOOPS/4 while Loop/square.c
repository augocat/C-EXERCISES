#include <stdio.h>

int main() {

//  int c = 0;
  int c = 9;
  int square = 0;

  //while (c <= 9) {
    //printf("%d\t%d\n", c, (c * c));
    //c++;
  //}

  while (c >= 0) {
    printf("%d\t%d\n", c, (c * c));
    c--;
  }
}